/**
 * Stub for @mediapipe/pose.
 * This app uses MoveNet, not BlazePose, so the actual MediaPipe Pose class
 * is never instantiated. This stub satisfies the static ESM import in
 * @tensorflow-models/pose-detection's bundle.
 */
export class Pose {}
